import isElectron from 'celyn/utils/is-electron';
import { module, test } from 'qunit';

module('Unit | Utility | is electron');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isElectron();
  assert.ok(result);
});
